import {pokemon_Arrey} from './app';

//console.log(pokemon_Arrey);
export default (arr) => {
	//console.log(arr);
	//console.log(container);


	let search = document.createElement('input');
	search.className = 'search';
	//search.addEventListener('onkeyup', filter());
}